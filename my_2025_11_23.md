# ply_to_splatv.py 改善前後の比較

## 概要比較

| 項目 | 改善前（初版） | 改善後（最終版） |
|------|--------------|----------------|
| **基本機能** | 複数PLY → splatv変換 | 同左 |
| **時間パラメータ生成** | ✅ 自動生成 | ✅ 自動生成 |
| **重要度ソート** | ❌ なし | ✅ あり |
| **カスタム時刻** | ❌ なし | ✅ あり |
| **packHalf2x16** | 単純実装 | 高速NumPy実装 |
| **進捗表示** | 基本的 | 詳細 |

---

## 詳細比較

### 1. 重要度ソート

**改善前:**
```python
# フレーム順にそのまま出力
for frame_idx, data in enumerate(all_data):
    for i in range(n_points):
        # 順番に書き込み
```

**改善後:**
```python
# opacity × volume で重要度計算してソート
importance = (
    sigmoid(opacity) * 
    np.exp(scale[:, 0]) * 
    np.exp(scale[:, 1]) * 
    np.exp(scale[:, 2])
)
sorted_indices = np.argsort(-importance)
```

**効果:** レンダリング品質向上（重要なGaussianを優先表示）

---

### 2. カスタム時刻配列

**改善前:**
```python
# 均等配置のみ
if n_frames > 1:
    trbf_center = frame_idx / (n_frames - 1)
```

**改善後:**
```python
# カスタム時刻対応 + 自動正規化
if frame_times is None:
    frame_times = np.linspace(0.0, 1.0, n_frames)
else:
    frame_times = np.array(frame_times)
    # 0.0-1.0に正規化
    frame_times = (frame_times - frame_times.min()) / 
                  (frame_times.max() - frame_times.min())
```

**使用例:**
```bash
# 最初のフレームを長く表示
python ply_to_splatv.py -i ./frames -o output.splatv \
  --frame_times "0,0.5,0.75,1.0"
```

---

### 3. packHalf2x16の高速化

**改善前:**
```python
def pack_half2x16(x, y):
    hx = int(np.float16(x).view(np.uint16))
    hy = int(np.float16(y).view(np.uint16))
    return (hx | (hy << 16)) & 0xFFFFFFFF
```

**改善後:**
```python
def pack_half2x16_batch(arr):
    """バッチ処理版（高速）"""
    n = x.shape[0]
    uint32_data = np.ndarray((n,), dtype=np.uint32)
    
    # NumPyのstrideで高速変換
    f16_data = np.lib.stride_tricks.as_strided(
        uint32_data.view(dtype=np.float16),
        shape=(2, n),
        strides=(1 * 2, 2 * 2),
        writeable=True,
    )
    f16_data[0] = x
    f16_data[1] = y
    return uint32_data
```

**効果:** 大量のGaussian処理が高速化

---

### 4. コマンドラインオプション

**改善前:**
```bash
python ply_to_splatv.py -i ./frames -o output.splatv
```

**改善後:**
```bash
python ply_to_splatv.py -i ./frames -o output.splatv \
  --temporal_sigma 0.3 \
  --frame_times "0,0.3,0.7,1.0" \
  --no_sort
```

| オプション | 説明 |
|-----------|------|
| `--temporal_sigma` | 時間幅調整（0.1=鋭い, 0.5=標準） |
| `--frame_times` | カスタム時刻配列 |
| `--no_sort` | 重要度ソート無効化 |

---

### 5. 出力ログの充実

**改善前:**
```
フレーム数: 6
総Gaussian数: 24,000
完了!
```

**改善後:**
```
============================================================
PLY to SPLATV Converter
============================================================
フレーム数: 6
Temporal sigma: 0.3
f_rest属性数: 9

[1/3] PLYファイルを読み込み中...
100%|████████████| 6/6

総Gaussian数: 24,000

[2/3] テクスチャデータを構築中...
テクスチャサイズ: 4096 x 6
フレーム時刻: [0.    0.167 0.389 0.556 0.722 1.   ]
temporal sigma: 0.016667
重要度ソート: True

重要度を計算中...
100%|████████████| 6/6
ソート完了: 24,000点

テクスチャを構築中...
100%|████████████| 24000/24000

[3/3] splatvファイルを書き出し中...

============================================================
完了!
============================================================
出力ファイル: output.splatv
ファイルサイズ: 1.47 MB
総フレーム数: 6
総Gaussian数: 24,000
テクスチャ: 4096 x 6

splatv viewerで開く:
  https://splatv.vercel.app/?url=YOUR_FILE_URL
```

---

## macaburguera版との比較

| 機能 | macaburguera版 | 改善後の私の版 |
|------|---------------|---------------|
| **入力形式** | SpacetimeGaussians PLY<br>（motion/omega含む） | 標準3DGS PLY<br>（motion/omegaなし） |
| **用途** | トレーニング済み変換 | **複数フレーム統合** ✅ |
| **時間パラメータ** | PLYに含まれる | **自動生成** ✅ |
| **重要度ソート** | ✅ | ✅ |
| **カスタム時刻** | ❌ | ✅ |
| **packHalf2x16** | 高速実装 | 同等の実装 |
| **あなたの要件** | ❌ 不適合 | ✅ **完全対応** |

---

## まとめ

### 改善点
1. ✅ **重要度ソート追加** - レンダリング品質向上
2. ✅ **カスタム時刻対応** - 柔軟な時間制御
3. ✅ **高速化** - 大量Gaussian処理の最適化
4. ✅ **詳細ログ** - デバッグと確認が容易

### あなたの要件への適合度
- **macaburguera版**: SpacetimeGaussians出力専用 → ❌
- **私の改善版**: 標準3DGS PLY複数枚から4D化 → ✅

**結論:** 改善版は、あなたの「複数PLYをスライドショー的に表示」という要件に完全に対応しています！🎆
